{
  "routes": [
    {
      "method": "GET",
      "path": "/formats",
      "handler": "formats.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/formats/count",
      "handler": "formats.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/formats/:id",
      "handler": "formats.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/formats",
      "handler": "formats.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/formats/:id",
      "handler": "formats.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/formats/:id",
      "handler": "formats.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
